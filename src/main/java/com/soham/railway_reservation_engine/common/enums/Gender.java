package com.soham.railway_reservation_engine.common.enums;

/**
 * Passenger gender — drives quota eligibility (e.g. Ladies quota) and the
 * senior-citizen age threshold (60 male / 58 female).
 */
public enum Gender {
    MALE,
    FEMALE,
    OTHERS
}
